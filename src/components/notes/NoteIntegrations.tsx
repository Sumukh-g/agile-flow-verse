import React from 'react';

const NoteIntegrations: React.FC<{ note: any; onUpdateNote: (noteId: string, updates: any) => void }> = () => {
  return <div className="p-6">Integrations view coming soon...</div>;
};

export default NoteIntegrations; 