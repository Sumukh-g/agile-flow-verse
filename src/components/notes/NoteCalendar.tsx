import React from 'react';

const NoteCalendar: React.FC<{ note: any; onUpdateNote: (noteId: string, updates: any) => void }> = () => {
  return <div className="p-6">Calendar view coming soon...</div>;
};

export default NoteCalendar; 