import React from 'react';

const NoteTimeline: React.FC<{ note: any; onUpdateNote: (noteId: string, updates: any) => void }> = () => {
  return <div className="p-6">Timeline view coming soon...</div>;
};

export default NoteTimeline; 