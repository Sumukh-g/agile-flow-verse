import React from 'react';

const NoteWhiteboard: React.FC<{ note: any; onUpdateNote: (noteId: string, updates: any) => void }> = () => {
  return <div className="p-6">Whiteboard view coming soon...</div>;
};

export default NoteWhiteboard; 