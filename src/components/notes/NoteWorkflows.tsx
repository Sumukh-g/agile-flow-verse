import React from 'react';

const NoteWorkflows: React.FC<{ note: any; onUpdateNote: (noteId: string, updates: any) => void }> = () => {
  return <div className="p-6">Workflows view coming soon...</div>;
};

export default NoteWorkflows; 